/* ============================================================
   OdontoPlus — Configuración Global (config.js)
   Modifica API_BASE para apuntar a tu backend
   ============================================================ */

const Config = {
    /** URL base del backend. Sin barra final. */
    API_BASE: 'http://localhost:5000',
};

window.Config = Config;
